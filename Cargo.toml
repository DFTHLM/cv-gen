[package]
name = "cv-gen"
version = "0.1.0"
edition = "2024"

[build-dependencies]
lalrpop = "0.20"

[dependencies]
lalrpop-util = { version = "0.21.0", features = ["lexer", "unicode"] }
regex = "1"

